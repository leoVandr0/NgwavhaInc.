import express from 'express';
const router = express.Router();

// Placeholder routes to prevent server crash until fully implemented
router.get('/', (req, res) => res.json({ message: 'Review route' }));

export default router;
