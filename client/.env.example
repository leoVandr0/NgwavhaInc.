VITE_API_URL=/api
# Optional: set when API (and Socket.IO) are on another host (e.g. production)
# VITE_SOCKET_URL=https://api.yourdomain.com

# WebRTC SFU signaling endpoint for live lessons (the VPS running sfu/).
# Dev:    http://localhost:4000
# Prod:   https://sfu.yourdomain.com   (TLS — browsers require WSS for getUserMedia)
# VITE_SFU_URL=http://localhost:4000
